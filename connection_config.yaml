# Connection configuration for Melanie's Smoothies
# Fill in values below. Do NOT commit secrets to source control.

database:
  host: localhost
  port: 5432
  user: KARINADZIEDZIC
  password: your_db_password
  name: your_db_name

api:
  base_url: https://api.example.com
  api_key: your_api_key_here

streamlit:
  secret_key: your_streamlit_secret_key
